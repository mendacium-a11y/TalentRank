# TalentRank AI

Local Dual-Mode Resume Screening App.

### Setup

1. Install requirements: `pip install -r requirements.txt`
2. Run backend: `uvicorn main:app --reload`
    - *Note: Uvicorn is a lightning-fast ASGI web server implementation for Python. It is used here to run our FastAPI application and handle the incoming HTTP web requests.*
